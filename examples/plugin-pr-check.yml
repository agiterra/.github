# Copy this file to .github/workflows/pr-check.yml in each plugin adapter repo
name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  plugin-check:
    uses: agiterra/.github/.github/workflows/plugin-check.yml@main
